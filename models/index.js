const { Contact, joiSchema, favoriteJoiSchema } = require('./contact');
const { User, schemas } = require('./user');

module.exports = {
  Contact,
  joiSchema,
  favoriteJoiSchema,
  User,
  schemas,
};
